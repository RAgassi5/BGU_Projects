#ifndef DEFS_H
#define DEFS_H

//an enum for returning a value for a bool function
typedef enum e_bool {false, true} bool;

//an enum function for returning a status if a function succeeded or failed
typedef enum e_status {success, failure} status;

#endif //DEFS_H
